PORT=4000
# MySQL local (when running without docker)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=secret
DB_NAME=rentdirect

JWT_SECRET=change_this_secret
JWT_EXPIRES_IN=7d

# Azure Blob (optional)
AZURE_BLOB_CONN=DefaultEndpointsProtocol=https;AccountName=...;AccountKey=...;EndpointSuffix=core.windows.net
AZURE_CONTAINER=rentdirect
NODE_ENV=development
